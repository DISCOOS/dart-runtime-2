name: dependency
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: '>=2.2.0 <3.0.0'

# Not intended for publishing
publish_to: none

dependencies:
  runtime_2:
    path: ../../..

dev_dependencies:
  pedantic: ^1.0.0
  test: ^1.0.0
